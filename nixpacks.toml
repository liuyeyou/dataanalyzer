[phases.setup]
aptPkgs = ["python3", "python3-dev", "python3-pip"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 -m streamlit run app.py" 